# wilnichols.com
Built with [Eleventy](https://www.11ty.dev). Prose and albums live in a private
vault repo, carried here as a submodule at `vault/` and reached through
symlinks so 11ty's data cascade still applies. See `AGENTS.md` for the
architecture and the rules that go with it.
