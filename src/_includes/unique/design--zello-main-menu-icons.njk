{# Extracted verbatim from the design post of the same name when work
   entries moved to the vault. Markup the workShot macro does not cover:
   a hand-written picture, or raw SVG includes.

   The SVG paths are relative to _includes, which is where this file now
   lives; they were ../../_includes/... when it sat in Design-Posts. #}
{# Desktop #}
<div class="picture">
  {% include "unique/zello-main-menu-icons.svg" %}
  {% include "unique/zello-main-menu-icons--mobile.svg" %}
</div>
