---
layout: page.njk
---
{% include 'title-and-date.njk' %}
{{ content | safe }}

{% set photos = page.fileSlug | getPhotos %}
{% set albumLength %}{{ photos | length }}{% endset %}

{% for photo in photos | reverse %}
  {% if photo !== ''%}
    {% set src %}{{ remote.gallery.base }}/{{ remote.gallery.photos }}/{{ page.fileSlug }}/{{ photo }}{% endset %}
    {# {% set imageProps = src | imageInfo %} #}
    {% if loop.index < 15 %}{% set loading = 'eager' %}{% else %}{% set loading = 'lazy' %}{% endif %}
    {{ src }}
    {# {{  Photo(
      el = 'li',
      fileName = photo | slugify,
      src = src,
      href = src,
      hasText = false,
      lightBoxGroup = page.fileSlug,
      imageProps = imageProps
    ) }} #}
  {% endif %}
{% endfor %}

{% include 'backlinks.njk' %}
